<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
        "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">
<hibernate-configuration>
    <session-factory>
        <!-- Database connection settings -->
        <property name="hibernate.dialect">org.hibernate.dialect.MySQLDialect</property>
        <property name="connection.url">jdbc:mysql://localhost:3306/cinema_db?serverTimezone=UTC</property>
        <property name="connection.driver_class">com.mysql.cj.jdbc.Driver</property>
        <property name="connection.username">root</property>
        <property name="connection.password">WailWay-Pass456</property>

        <!-- Connection pool settings -->
        <property name="hibernate.connection.pool_size">10</property>

        <!-- Echo all executed SQL to stdout -->
        <property name="show_sql">true</property>
        <property name="format_sql">true</property>

        <!-- Automatically create/update database schema -->
        <property name="hbm2ddl.auto">create-drop</property>

        <!-- Entity mappings -->
        <mapping class="mate.academy.model.Movie"/>
        <mapping class="mate.academy.model.CinemaHall"/>
        <mapping class="mate.academy.model.MovieSession"/>
        <mapping class="mate.academy.model.User"/>
    </session-factory>
</hibernate-configuration>
